version: '3.8'

services:
  search-engine:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mini-search-engine
    ports:
      - "8080:8080"
    restart: unless-stopped
    environment:
      - RUST_LOG=info
